// Conta ATIVA no cliente (white-label). Hoje só a IA Club está conectada, então
// é uma constante — quando houver login/multi-conta de verdade, isto vem da
// sessão/seletor de conta. Mantém o mesmo UUID do FALLBACK_CONTENT_ACCOUNT_ID
// (lib/tenant.ts, lado servidor) = instagram_accounts da iaclubcomunidade.
export const CURRENT_ACCOUNT_ID = "84391c32-d32b-41f1-a8f0-67f929c9fb0b";
